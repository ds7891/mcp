<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 本工具核心用途：枚举本机已安装应用以做同签名比对（Android 11+ 包可见性） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- targetSdk 28+ 默认禁止明文 HTTP；本工具需要访问 http 端点
         （本地/局域网的 OpenAI 兼容服务、以及 127.0.0.1 环回桥接），故显式放开。 -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:name="com.mcp.injector.InjectorApp"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MCPInjector">

        <activity
            android:name="com.mcp.injector.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.mcp.injector.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name="com.mcp.injector.crash.CrashLogActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MCPInjector" />
    </application>
</manifest>
